# Case 05 intentionally has no official no-formal UCAgent flow.
# Use `config_full_demo.yaml` through `bash scripts/run_cases.sh --case 05`.
#
# This placeholder is kept only to make the absence explicit for readers who
# inspect the workspace. The public runner rejects `--case 05 --no-formal`.
