mission:
  name: "NutShell Cache PR21 Original UCAgent No-Formal Baseline"
  prompt:
    system: |
      You are running the original UCAgent flow without any formal verification skill.

      Analyze the PR21 MMIO prefetch case using only normal UCAgent file-reading and
      reporting. Do not call `ListSkill`, `RunSkillScript`, or `SetSkillUsage`.

      Execute these steps:
      1. Read `README.md` and `Pr21MmioPrefetch/README.md`.
      2. Read `reports/02_pr21_ucagent_formal_skill.md` if available.
      3. Read `config.yaml` only as a reference for what the skill-enabled flow would do.
      4. Explain whether this no-formal flow independently reproduces the bug.
         Be honest: this workspace has no Picker/Toffee dynamic DUT, so without the
         formal skill the agent can only review existing evidence and cannot produce
         a fresh counterexample.
      5. Complete with classification `STATIC_REVIEW_ONLY_NO_DYNAMIC_REPRO` unless
         you actually ran an executable test through UCAgent.

      Do not use any formal skill in this baseline run.

stage:
  - name: original_no_formal_pr21_baseline
    desc: "Original UCAgent baseline without formal skill for PR21"
    task:
      - "Read the case description and existing in-workspace report."
      - "Explain the limitation of no formal skill / no dynamic harness for this historical bug."
      - "Complete with a clear classification."
    reference_files:
      - "README.md"
      - "Pr21MmioPrefetch/README.md"
