# src

通用验证工具目录。

```text
src/formal/verify_verilog.py                 任意 RTL lint/formal smoke 入口
src/lib/collect_ucagent_token_usage.py       UCAgent 日志 token 汇总 helper
src/ucagent_skills/generic-formal/           UCAgent 通用 formal skill
```

`src/` 不放具体测试案例。案例资产位于 `tests/cases/`。
